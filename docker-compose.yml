version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: conversation-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: conversation-service
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
